boclips:
  lti:
    gateway:
      services:
        apiUrl: 'https://api.staging-boclips.com'

keycloak:
  configurationFile: 'classpath:keycloak-staging.json'

spring:
  mvc:
    favicon:
      enabled: false
