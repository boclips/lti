boclips:
  lti:
    gateway:
      services:
        apiUrl: 'https://api.testing-boclips.com'

keycloak:
  configurationFile: 'classpath:keycloak-testing.json'

spring:
  mvc:
    favicon:
      enabled: false
